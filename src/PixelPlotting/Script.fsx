// First
#load "PixelPlotter.fs"

// Second
open PixelPlotting.Plotter // Open the module
open System.Drawing // open so we can access the colors easily

// Fourth - Create some plot points
//bitmap.SetPixel(0,0,Color.Yellow)
//bitmap.SetPixel(1,0,Color.Yellow)
//bitmap.SetPixel(0,1,Color.Yellow)
//bitmap.SetPixel(1,1,Color.Yellow)

// Fifth - Create smiley
//bitmap.SetPixel(6,3, Color.Black)
//bitmap.SetPixel(7,5, Color.Black)
//bitmap.SetPixel(8,3, Color.Black)
//bitmap.SetPixel(5,6, Color.Black)
//bitmap.SetPixel(6,7, Color.Black)
//bitmap.SetPixel(7,7, Color.Black)
//bitmap.SetPixel(8,7, Color.Black)
//bitmap.SetPixel(9,6, Color.Black)

// Third
saver bitmap // call our saver function passing in the bitmap we have manipulated